---
phase: 03-static-pages
plan: 01
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: [src/routes/pages.tsx, src/components/Footer.tsx]
autonomous: false
---

<objective>
## Goal
Port the About and Contact pages from the original site to server-rendered Hono JSX, replacing placeholder routes with full content parity.

## Purpose
These are the two static bio/contact pages that complete Chris's personal web presence. No API calls or database queries needed — pure server-rendered content.

## Output
- `src/routes/pages.tsx` — About and Contact routes replaced with full content
- `src/components/Footer.tsx` — Extracted Footer component (shared by all pages, needed for contact link in footer)
- Layout.tsx updated to use Footer component
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/02-database-auth/02-01-SUMMARY.md
- Phase 1 established Layout, Nav, homepage route, intro animation
- Phase 2 established D1 schema and auth (not directly used in this plan)

## Source Files
@src/routes/pages.tsx (current placeholder routes)
@src/components/Layout.tsx (shared layout with headExtra/bodyExtra pattern)
@src/components/Nav.tsx (navigation component)
@about.html (original about page — full content reference)
@contact.html (original contact page — full content reference)
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /using-git-worktrees | required | Before starting feature branch work | ○ |
| /finishing-a-development-branch | required | When implementation is done | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /using-git-worktrees loaded
- [ ] /finishing-a-development-branch loaded (at completion)
</skills>

<acceptance_criteria>

## AC-1: About Page Content Parity
```gherkin
Given the Hono dev server is running
When I navigate to /about
Then I see the full About page with: section header, photo sidebar with quick facts, background text, career timeline (5 entries), achievement spotlight (3 tiles), and connect section (email, LinkedIn, phone cards)
```

## AC-2: Contact Page Content Parity
```gherkin
Given the Hono dev server is running
When I navigate to /contact
Then I see the full Contact page with: section header, contact info sidebar (email, LinkedIn, phone links), and the contact form (name, email, subject, message fields + submit button)
```

## AC-3: Footer Component Extracted
```gherkin
Given any page is rendered
When I view the footer
Then I see the copyright notice and footer navigation links matching the original site
And the Footer component is shared across all pages via Layout.tsx
```

## AC-4: Navigation Active State Correct
```gherkin
Given I navigate to /about or /contact
When the page renders
Then the corresponding nav link has the "active" class applied
```

## AC-5: TypeScript Compiles
```gherkin
Given all changes are complete
When I run npx tsc --noEmit
Then there are zero TypeScript errors
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer component and port About + Contact pages</name>
  <files>src/components/Footer.tsx, src/components/Layout.tsx, src/routes/pages.tsx</files>
  <action>
    1. Create `src/components/Footer.tsx`:
       - Extract footer from Layout.tsx into its own component
       - Include copyright, footer-links list (Home, About, My Gear, Reading List)
       - Keep it simple — no now-playing widget yet (that's a future enhancement)

    2. Update `src/components/Layout.tsx`:
       - Import and use Footer component instead of inline footer JSX

    3. Replace the `/about` placeholder route in pages.tsx with full content:
       - Port all content from about.html:
         - Section header with eyebrow ("// about") + "Background & Experience" heading
         - Two-column about-layout: photo sidebar (image, quick facts card, CTA button) + main content
         - Background and "At Aseva" paragraphs
         - Skills section: skill pills (lg/md/sm tiers) + tech stack grid (SASE, MDR, Networking, SD-WAN, Collaboration)
         - Career Timeline section (5 entries: VP Aseva, VP Client Services, Director of Sales, Sales Director Netlojix, Sales Executive Netlojix)
         - Achievement Spotlight (40 Under 40, 20+ Years, 50+ CIOs)
         - Connect section (email, LinkedIn, phone cards)
       - Use Layout component with appropriate title, description, currentPath="/about"
       - Add GSAP + ScrollTrigger to headExtra (needed for animations)
       - Add animations.js to bodyExtra

    4. Replace the `/contact` placeholder route in pages.tsx with full content:
       - Port all content from contact.html:
         - Section header with eyebrow ("// contact") + "Let's Connect" heading
         - Two-column contact-layout: info sidebar (intro text, contact links with SVG icons, response time note) + form
         - Contact form: name, email, subject, message fields + submit button
         - Form action: use formspree.io placeholder (same as original)
       - Use Layout component with appropriate title, description, currentPath="/contact"
       - Add GSAP + ScrollTrigger to headExtra
       - Add animations.js to bodyExtra

    Avoid:
    - Do NOT add the contact link to the Nav component (original site nav does not always show Contact)
    - Do NOT modify the homepage route — it's already complete from Phase 1
    - Do NOT add now-playing widget to Footer (original uses Last.fm API — future enhancement)
    - Do NOT add command palette (JS-heavy, not needed for SSR pages yet)
    - Do NOT add easter-egg.js or other client-side scripts beyond animations
  </action>
  <verify>
    1. `npx tsc --noEmit` passes with zero errors
    2. `npx wrangler dev` starts without errors
    3. Navigate to /about — full content visible
    4. Navigate to /contact — full content visible
    5. Footer appears on all pages
  </verify>
  <done>AC-1, AC-2, AC-3, AC-5 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>About and Contact pages with full content parity from the original site, plus extracted Footer component.</what-built>
  <how-to-verify>
    1. Run: `cd .worktrees/03-static-pages && npx wrangler dev`
    2. Visit: http://localhost:8787/about
       - Confirm: Photo, quick facts, background text, skills pills, tech stack grid, career timeline (5 entries), achievement tiles, connect cards
    3. Visit: http://localhost:8787/contact
       - Confirm: Contact info sidebar with links, contact form with 4 fields
    4. Visit: http://localhost:8787/ (homepage)
       - Confirm: Footer still renders correctly, nav works
    5. Check nav active states on /about and /contact
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/db/* (schema and queries — locked from Phase 2)
- src/lib/* (password and session libs — locked from Phase 2)
- src/middleware/* (auth middleware — locked from Phase 2)
- src/routes/api/* (auth API — locked from Phase 2)
- src/components/IntroOverlay.tsx (Phase 1 — no changes)
- Homepage route in pages.tsx (Phase 1 — no changes)
- src/types.ts (no new types needed for static pages)

## SCOPE LIMITS
- No gear/uses page in this plan (that's Plan 02)
- No database queries (static content only)
- No command palette, easter eggs, or theme toggle JS
- No now-playing/Last.fm widget in footer
- No reading-list route changes

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` — zero errors
- [ ] `npx wrangler dev` — starts without errors
- [ ] /about renders full content matching about.html
- [ ] /contact renders full content matching contact.html
- [ ] Footer component renders on all pages
- [ ] Nav active states work for /about and /contact
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- About page has full content parity with about.html
- Contact page has full content parity with contact.html
- Footer extracted as shared component
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.paul/phases/03-static-pages/03-01-SUMMARY.md`
</output>
